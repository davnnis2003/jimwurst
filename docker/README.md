# docker

Centralized orchestration and environment management for the local DWH stack.

## Contents
- **docker-compose.yml**: The main orchestration file to spin up the core stack (Postgres).
- **.env.example**: Template for environment variables needed across all applications.

This folder serves as the "entry point" for running the entire system locally.
